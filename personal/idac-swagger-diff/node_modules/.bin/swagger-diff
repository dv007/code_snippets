#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../swagger-diff/bin/swagger-diff.js" "$@"
  ret=$?
else 
  node  "$basedir/../swagger-diff/bin/swagger-diff.js" "$@"
  ret=$?
fi
exit $ret
